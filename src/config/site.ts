import { type SiteConfig } from "~/types"

export const siteConfig: SiteConfig = {
  name: "Evento",
  description: "our Event with Us",
  url: "",
  ogImage: "",
}

export const miniSiteContent = {
  registerQuote: `${siteConfig.name} makes it really easy to create Events and Manage and invite the world to join you.`,
  welcomeQuote: `Let's create new Events`,
}
